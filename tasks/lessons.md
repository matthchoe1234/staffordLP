# lessons

- Notion MCP `query-data-sources` has a workspace quota. Primitives and Semantics came through,
  then it capped. Fallback that worked: `notion-search` scoped to a data source URL to get row
  page IDs, then `notion-fetch` each row (no quota), plus summary pages that restate tokens
  (Sell Sheet Design Direction, Figma Styles Library task).
- The live Shopify theme (t/139/assets/style.css) hosts every Exposure optical cut and Monument
  Mono weight as woff2. That is the fastest legal source for webfonts already in web use.

- Raw HTML captures of Shopify pages embed storefront access tokens. gitleaks blocked the first commit.
  assets/ref/*.html and *.css are gitignored; keep captures local, commit only what was extracted.
